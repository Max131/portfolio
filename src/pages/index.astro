---
import Layout from "@layouts/Default.astro";
import { defaultLang as defaultLocale, site, contactItems } from "@i18n/ui.ts";
import { useTranslation } from "@i18n/utils.ts";
import Projects from "@components/sections/Projects.astro";

const currentLocale = Astro.currentLocale || defaultLocale;
const t = useTranslation(currentLocale);
---

<Layout title={t("user.role")}>
	<div class="max-w-5xl mx-auto p-4 lg:px-0">
		<section id="hero">
			<div
				class="max-w-3xl my-12 px-4 py-2 lg:px-0 border-l-4 border-cyan-400 bg-cyan-50/50"
			>
				<div class="italic px-4" set:html={t("hero.content")} />
			</div>
		</section>

		<div class="mt-16">
			<Projects />
		</div>

		<section class="mb-12" id="about">
			<h2 class="font-title font-bold mb-1 text-2xl">{t("about.title")}</h2>
			<div class="text-lg *:[p]:mb-4" set:html={t("about.content")} />
		</section>

		<section id="contact" class="mb-12 bg-white p-6 rounded-xl shadow-lg">
			<h2 class="font-title font-bold mb-1 text-2xl">{t("contact.title")}</h2>
			<div
				class="text-lg text-stone-500 *:[p]:mb-4"
				set:html={t("contact.content")}
			/>
			<ul class="mt-6">
				{
					contactItems.map((item) => (
						<li>
							<a
								class="inline-flex gap-1 items-base px-6 py-3 rounded-lg bg-blue-400 text-white"
								href={item.link}
								target="_blank"
								rel="noopener noreferrer"
							>
								<span>
									<item.icon />
								</span>
								<span class="font-medium text-xl">{item.name}</span>
							</a>
						</li>
					))
				}
			</ul>
		</section>
	</div>
</Layout>
